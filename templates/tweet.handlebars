<div class="tweet" data-tweet-id={{_id}} data-tweet-username={{username}}>
    <h4>@{{username}}</h4>
    <h6>{{tweetedBy username creator}}</h6>
    <p>{{content}}</p>
    <a href="#" class="edit-tweet">Edit</a>
    <a href="#" class="delete-tweet">Delete</a>
</div>